<Window x:Class="BreadFactory.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Имя пользователя:" Grid.Row="0"/>
        <TextBox Text="{Binding Username}" Grid.Row="1" Margin="0,5,0,15"/>

        <TextBlock Text="Пароль:" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,5,0,15"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                   Grid.Row="4" TextWrapping="Wrap"/>

        <Button Content="Войти" Command="{Binding LoginCommand}"
                Grid.Row="5" Width="100" HorizontalAlignment="Center"/>
    </Grid>
</Window>